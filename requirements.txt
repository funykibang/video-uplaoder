# Core pipeline
anthropic>=0.40
feedparser>=6.0
beautifulsoup4>=4.12
newspaper3k>=0.2.8
trafilatura>=1.8
requests>=2.31
cloudscraper>=1.2.71
python-dotenv>=1.0

# Media processing
faster-whisper>=1.0.0
ffmpeg-python>=0.2.0
Pillow>=10.0
pydub>=0.25.1

# Uploader (third-party posting service)
upload-post>=0.1

# Scheduler
schedule>=1.2

# Demo UI (optional)
flask>=3.0

# Testing
pytest>=8.0
pytest-cov>=5.0

# ── Optional TTS engines (only needed if TTS_ENGINE != "elevenlabs") ──────────
# TTS>=0.22.0          # coqui
# piper-tts>=1.2.0     # piper
